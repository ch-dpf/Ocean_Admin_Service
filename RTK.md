# 仓库开发约束

## 测试代码生命周期

- 每次生成或调整生产代码后，必须执行与改动相匹配的测试。
- 为本次验证而新增或调整的测试类仅作为临时验证代码使用；测试完成并确认结果后，必须自行删除这些测试类。
- 不得将测试类提交到仓库。仓库通过 `.gitignore` 忽略各模块的 `src/test/` 目录。
- 删除测试类不等于跳过测试；交付说明中必须明确实际执行的测试及其结果。

## 开发库数据库脚本管理

- 本仓库属于开发阶段仓库，平台业务模块与 GIS 业务模块的数据库对象必须按 PostgreSQL Schema 分开维护，不得跨 Schema 混写。
- `ocean_platform` Schema 的表、索引、约束、注释及初始化数据统一维护在 `db/migration/V1__init_ocean_platform.sql`。
- `ocean_gis` Schema 的表、索引、约束、注释及初始化数据统一维护在 `db/migration/V2__init_ocean_gis.sql`。
- 每个业务模块只维护一个对应 Schema 的 SQL 脚本；调整已有模块数据库结构时直接更新该脚本，不得为同一模块继续新增增量迁移脚本。
- 涉及 `ALTER TABLE`、字段或约束调整、数据回填等用于改造现有开发数据库的过渡 SQL，只允许临时执行，不得保存为独立 SQL 文件或提交到仓库。
- 过渡 SQL 执行后，必须把变更后的最终表结构、索引、约束、注释及必要初始化数据完整合并到对应业务 Schema 的唯一 SQL 脚本中，确保新环境可直接得到最终结构。
- SQL 脚本调整后，必须在仓库根目录依次执行 `mvn flyway:repair` 和 `mvn flyway:validate`，先修复开发数据库中的迁移校验信息，再验证脚本与数据库迁移历史一致。
- 若 Flyway 命令因数据库不可用、连接配置或权限问题无法完成，交付说明必须明确失败命令、原因及尚未验证的风险。

## 代码复用与文件新增

- 实现功能前必须先检索现有代码，优先复用或扩展职责相符的方法和类，避免重复实现。
- 可在同一职责范围内复用的逻辑应抽取为现有类中的公共方法；确需跨类、跨模块复用时，应优先复用或扩展现有公共类。
- 尽量通过修改现有文件完成需求，不得为拆分形式、局部便利或一次性逻辑随意新增文件。
- 新增任何生产代码、测试代码或代码配套文件前，必须先说明拟新增文件的路径、职责及无法复用现有文件的原因，并取得用户本人明确确认后方可创建。
- 未获得明确确认时，只能调整现有文件；不得先创建文件后补充说明。

## 代码简洁性与可读性

- 代码实现应保持简洁、清晰，优先采用易于理解和维护的直接实现，避免无必要的抽象、嵌套与复杂控制流程。
- 类、方法、变量及常量命名必须准确表达职责和含义，避免含糊缩写、无意义命名及与实际用途不符的名称。
- 方法应职责单一，复杂逻辑应通过合理拆分、提前返回或复用现有公共逻辑降低阅读负担。
- 注释用于说明业务意图、边界条件或不直观的设计原因，不得用注释重复描述代码本身。
- 修改代码时应同步清理本次改动直接涉及的冗余代码、无效分支及无用导入，保持统一的现有代码风格。
