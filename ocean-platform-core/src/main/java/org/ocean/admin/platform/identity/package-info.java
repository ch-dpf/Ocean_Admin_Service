/**
 * Identity bounded context: users, platforms, scoped RBAC, credentials and sessions.
 * Other modules must use org.ocean.admin.platform.api instead of internal classes.
 */
package org.ocean.admin.platform.identity;

